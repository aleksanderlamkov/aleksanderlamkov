@import "../mixins.pcss";

ul[class]:not(.ul),
ol[class]:not(.ol) {
  list-style: none;
}

ol:not([class]),
.ol,
ul:not([class]),
.ul {
  --paddingLeft: 20px;

  li {
    padding-left: var(--paddingLeft);
  }
}

ul:not([class]),
.ul {
  li {
    --markerSize: 5px;

    position: relative;

    &::before {
      @mixin square var(--markerSize);

      content: "";
      position: absolute;
      top: 0.5em;
      left: 0;
      display: block;
      background-color: currentColor;
      border-radius: 50%;
    }
  }
}

ol:not([class]),
.ol {
  --paddingLeft: 28px;

  counter-reset: custom-counter;

  li {
    position: relative;

    &::before {
      position: absolute;
      right: calc(100% - 14px);
      font-weight: 700;
      content: counters(custom-counter, ".") ". ";
      counter-increment: custom-counter;
    }
  }
}
