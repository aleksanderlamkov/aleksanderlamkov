.i-icon {
  --color: var(--c-dark);
  --size: 1em;

  display: inline-flex;
  justify-content: center;
  align-items: center;
  width: var(--size);
  height: var(--size);

  * {
    fill: var(--color);
  }
  
  &.icon-webpack {
    path:nth-child(1) {
      fill: transparent;
    }
  }
}
